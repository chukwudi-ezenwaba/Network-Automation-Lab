# 
ip address show

#
sudo apt install net-tools 

# 
netstat -i

# 
ifconfig

# display interface table
netstat -i 

# display all listening connections
netstat -l 

# display only TCP listening connections
netstat -lt 

# -t for TCP, -u for UDP, -n for numeric (port number), and -a for all connected sockets
netstat -tuna 